# ga4datactl

The `ga4datactl` PyPI distribution will be a thin launcher for the GA4 Data CLI
provided by `marketing-toolbox`. After the first release has been configured and
published to PyPI, run:

```bash
uvx ga4datactl --help
```
