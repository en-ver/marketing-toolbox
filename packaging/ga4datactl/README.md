# ga4datactl

`ga4datactl` installs the GA4 Data command backed by
[`marketing-toolbox`](https://github.com/en-ver/marketing-toolbox#readme).

```bash
uvx ga4datactl --help
uv tool install ga4datactl
```
