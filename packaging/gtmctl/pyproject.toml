[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "gtmctl"
version = "0.2.0"
description = "Thin launcher for the marketing-toolbox GTM CLI."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
dependencies = ["marketing-toolbox==0.2.0"]

[project.scripts]
gtmctl = "gtmctl.cli:main"

[tool.uv.sources]
marketing-toolbox = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/gtmctl_alias"]
