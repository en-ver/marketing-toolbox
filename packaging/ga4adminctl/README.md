# ga4adminctl

The `ga4adminctl` PyPI distribution will be a thin launcher for the GA4 Admin CLI
provided by `marketing-toolbox`. After the first release has been configured and
published to PyPI, run:

```bash
uvx ga4adminctl --help
```
